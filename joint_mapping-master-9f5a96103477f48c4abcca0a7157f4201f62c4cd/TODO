
TODO LIST


1. Separate the package node into different types of packages
2. Make the knn matcher return histogram with weights as part of it so that
   we can find the peak with weighted average rather than counts
